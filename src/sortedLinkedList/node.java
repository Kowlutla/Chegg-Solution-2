package sortedLinkedList;


public class node
{
   String name; //to store the node's data
   node next; //pointer used to point to the next node in the list
  
   //Constructor
   public node(String d)
  
   {
       name = d;
       next = null;
   }
}